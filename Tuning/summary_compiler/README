Resumo:
Módulo responsável pela compilacao de todos os resultados obtidos de um data set em apenas um arquivo .csv

Arquivos:
->summary_compiler.R (e .sh e .sub)
Script(Job) utilizando para a compilacao de cada data set.

->generate_summary_dir_list.sh
Script auxiliar, nao precisa ser usado manualmente. Gera uma lista de pastas cujo conteudo sao os os arquivos de summary

->summary_dir_list
Lista gerada pelo script generate_summary_dir_list.sh


Instrucoes de uso:
Esse módulo depende de execucao dos jobs do módulo "individual_tuning", pois presupoem a existencia de dos dados obtidos pelo tuning dos datasets. 
O job "summary_compilar" executa para cada um dos datasets uma busca pelos seus arquivos de summary e os compila em um único csv, que é salvo  na pasta do dataset.
Observacao: Como os arquivos individuais de summary só tem como responsábilidade calcular e armazenar aquilo que calcularam, este módulo tem como responsábilidade fazer a compilacao respeitando a formatacao final de colunas esperada.
